/** Server and client defaults for fields removed from beta v1 Step 2 UI. */
export const DEFAULT_DINNERS_WANTED = 3;
/** High default so ranking is not constrained by a removed UI field. */
export const DEFAULT_MAX_INGREDIENTS = 20;
export const DEFAULT_PLANNING_MODE = "ingredient-first" as const;
