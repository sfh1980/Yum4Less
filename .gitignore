# Dependencies and build output
node_modules/
.next/
out/

# Local environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Private learning notes for the repo owner
.private/

# Local MCP config may contain user-scoped tokens — copy from mcp.json.example
.cursor/mcp.json

# Credentials and key material
*.pem
credentials.json
secrets/

# Local weekly-ad capture artifacts from failed live ingest runs
captures/
.playwright-mcp/
playwright-report/
test-results/
blob-report/

# Local Semgrep full-repo audit JSON (see README "Semgrep CI on GitHub Actions")
semgrep-full.json

# Local ingest/API probe logs (not source of truth)
live-probe-*.txt
playwright-full-test-final.png
