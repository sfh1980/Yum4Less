# Dependencies and build output (rebuilt inside the image)
node_modules
.next
out

# Git / editor / local tooling
.git
.github
.cursor
.private
.vscode
*.md
!README.md

# Local env and secrets — inject at runtime via compose env_file / environment
.env
.env.*
!.env.example

# Tests and e2e (not needed in production image)
e2e
coverage
playwright-report
test-results
blob-report
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
vitest.config.*
playwright.config.*

# Local data / capture / backup artifacts
backups
captures
.playwright-mcp
semgrep-full.json
*.png
*.csv
.tmp-*
live-probe-*.txt
.tsc-*.txt
.cursor-tsc-*.txt

# Docs beyond what's needed to build (keep tree small)
docs
scripts
db
